# Phase 설정 (local, alpha)
PHASE=local

# Database Configuration
# 보안: DB 정보는 파일에 저장하지 않음
# AWS Secret Manager "prod/ignite-pilot/postgresInfo2"에서 자동으로 로드됨
# DATABASE_URL 환경 변수는 사용하지 않음

# Server Ports (Phase별로 다를 수 있음)
API_PORT=8101
MCP_PORT=8102
FRONTEND_PORT=8100

# API Security (선택사항 - 설정하면 API 키 인증 활성화)
# Alpha 환경에서는 필수 권장
# API_KEY=your-secret-api-key-here

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Local: DEBUG, Alpha: INFO 권장
LOG_LEVEL=INFO
